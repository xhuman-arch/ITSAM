@model int

<a class="block px-2 py-1 text-body2 hover:text-primary relative" asp-controller="Notification" asp-action="Index">
    Notifikasi
    @if (Model > 0)
    {
        <span class="badge-danger ml-1">@Model</span>
    }
</a>
